FROM python:3.11-slim

WORKDIR /app

# install dependencies
COPY requirements/scheduler.txt ./scheduler.txt
RUN pip install --no-cache-dir -r scheduler.txt

# copy script
COPY src/ /app/

CMD ["python", "push_to_sheet.py"]